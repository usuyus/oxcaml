(* TEST
 include stdlib_stable;
 include stdlib_beta;
 include stdlib_upstream_compatible;
 modules = "idx_repr.ml stubs.c";
 flags = "-extension layouts_alpha";
 bytecode;
*)
(** This is code generated by [test_generation.ml]. *)

[@@@warning "-23"]
type unit_u : void mod everything
external unbox_unit : unit -> unit_u = "%unbox_unit"
external globalize : local_ 'a -> 'a = "%obj_dup";;
let int64x2_u_equal (_ : int64x2#) (_ : int64x2#) = failwith "should not be called from bytecode"

open Stdlib_upstream_compatible
open Stdlib_stable
open Stdlib_beta

external[@layout_poly] makearray_dynamic_local :
  ('a : any mod separable) . int -> 'a -> 'a array @ local =
  "%makearray_dynamic"

external[@layout_poly] makearray_dynamic :
  ('a : any mod separable) . int -> 'a -> 'a array =
  "%makearray_dynamic"

external[@layout_poly] get :
  ('a : any mod separable) . ('a array[@local_opt]) -> (int[@local_opt]) -> 'a =
  "%array_safe_get"

external[@layout_poly] set :
  ('a : any mod separable) . ('a array[@local_opt]) -> (int[@local_opt]) -> 'a -> unit =
  "%array_safe_set"

let failwithf fmt = Printf.ksprintf failwith fmt

(* Redefine iter to infer locality *)
let rec iter ~f = function
    [] -> ()
  | a::l -> f a; iter ~f l

module Int_set = Set.Make(Int)

let tests_run = ref Int_set.empty

let mark_test_run test_id =
  tests_run := Int_set.add test_id !tests_run

(* Various interesting values *)

let sizes = [ 0; 1; 2; 30 ]

let indices_in_deepening_tests = [0; 100_000]

type packed = P : 'a -> packed
let ref_to_force_heap_allocation : packed ref = ref (P 0)

type t0 = { mutable a0 : int; mutable b0 : int } (* { int; int } *)
type t1 = { mutable a1 : int; mutable b1 : int; mutable c1 : float } (* { int; int; float } *)
type t2 = { mutable a2 : int; mutable b2 : int32# } (* { int; int32# } *)
type t3 = #{ a3 : int; b3 : int32# } (* #{ int; int32# } *)
type t4 = { mutable a4 : int; mutable b4 : t3 } (* { int; #{ int; int32# } } *)
type t5 = #{ a5 : int32# } (* #{ int32# } *)
type t6 = { mutable a6 : int; mutable b6 : t5 } (* { int; #{ int32# } } *)
type t7 = #{ a7 : float; b7 : float } (* #{ float; float } *)
type t8 = { mutable a8 : int; mutable b8 : t7 } (* { int; #{ float; float } } *)
type t9 = { mutable a9 : int64#; mutable b9 : nativeint# } (* { int64#; nativeint# } *)
type t10 = { mutable a10 : int32# } (* { int32# } *)
type t11 = { mutable a11 : int32#; mutable b11 : int } (* { int32#; int } *)
type t12 = { mutable a12 : int32#; mutable b12 : int; mutable c12 : int } (* { int32#; int; int } *)
type t13 = { mutable a13 : int32#; mutable b13 : int32# } (* { int32#; int32# } *)
type t14 = #{ a14 : int32#; b14 : int32# } (* #{ int32#; int32# } *)
type t15 = { mutable a15 : int32#; mutable b15 : t14 } (* { int32#; #{ int32#; int32# } } *)
type t16 = { mutable a16 : nativeint#; mutable b16 : nativeint# } (* { nativeint#; nativeint# } *)
type t17 = { mutable a17 : float } (* { float } *)
type t18 = { mutable a18 : float; mutable b18 : int } (* { float; int } *)
type t19 = { mutable a19 : float; mutable b19 : int; mutable c19 : int } (* { float; int; int } *)
type t20 = { mutable a20 : float; mutable b20 : int; mutable c20 : float } (* { float; int; float } *)
type t21 = { mutable a21 : float; mutable b21 : int32# } (* { float; int32# } *)
type t22 = { mutable a22 : float; mutable b22 : float; mutable c22 : float } (* { float; float; float } *)
type t23 = { mutable a23 : string; mutable b23 : string } (* { string; string } *)
type t24 = { mutable a24 : string; mutable b24 : string; mutable c24 : string } (* { string; string; string } *)
type t25 = #{ a25 : int } (* #{ int } *)
type t26 = { mutable a26 : t25 } (* { #{ int } } *)
type t27 = { mutable a27 : t25; mutable b27 : int } (* { #{ int }; int } *)
type t28 = #{ a28 : int; b28 : int } (* #{ int; int } *)
type t29 = { mutable a29 : t28; mutable b29 : int } (* { #{ int; int }; int } *)
type t30 = #{ a30 : int; b30 : float } (* #{ int; float } *)
type t31 = { mutable a31 : t30 } (* { #{ int; float } } *)
type t32 = { mutable a32 : t5; mutable b32 : int } (* { #{ int32# }; int } *)
type t33 = { mutable a33 : t5; mutable b33 : int32# } (* { #{ int32# }; int32# } *)
type t34 = { mutable a34 : t14; mutable b34 : int } (* { #{ int32#; int32# }; int } *)
type t35 = #{ a35 : float } (* #{ float } *)
type t36 = { mutable a36 : t35; mutable b36 : int } (* { #{ float }; int } *)
type t37 = { mutable a37 : t35; mutable b37 : float } (* { #{ float }; float } *)
type t38 = { mutable a38 : t35; mutable b38 : float# } (* { #{ float }; float# } *)
type t39 = { mutable a39 : t7 } (* { #{ float; float } } *)
type t40 = { mutable a40 : t7; mutable b40 : float } (* { #{ float; float }; float } *)
type t41 = #{ a41 : float; b41 : float# } (* #{ float; float# } *)
type t42 = { mutable a42 : t41; mutable b42 : float } (* { #{ float; float# }; float } *)
type t43 = #{ a43 : float#; b43 : float } (* #{ float#; float } *)
type t44 = { mutable a44 : t43; mutable b44 : float# } (* { #{ float#; float }; float# } *)
type t45 = #{ a45 : string; b45 : string } (* #{ string; string } *)
type t46 = { mutable a46 : t45; mutable b46 : string } (* { #{ string; string }; string } *)

let to_run () =
  (*************************)
  (*   t0 = { int; int }   *)
  (*************************)
  let r = stack_ { a0 = 0; b0 = 1 } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a0 *)
  let actual = r.a0 in
  let expected = 0 in
  mark_test_run 1;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 1 failed";
  (* Paths of depth 1 *)
  (* .b0 *)
  let actual = r.b0 in
  let expected = 1 in
  mark_test_run 2;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 2 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a0 = a01; b0 = b01 } { a0 = a02; b0 = b02 } -> (fun a b -> Int.equal a b) a01 a02 && (fun a b -> Int.equal a b) b01 b02) in
  let next_r = stack_ { a0 = 100; b0 = 101 } in
  let r_expected = stack_ { a0 = 0; b0 = 1 } in
  (* .a0 *)
  r.a0 <- next_r.a0;
  let r_expected = { r_expected with a0 = next_r.a0 } in
  mark_test_run 3;
  let test = eq r r_expected in
  if not test then failwithf "test 3 failed";
  Gc.compact ();
  (* .b0 *)
  r.b0 <- next_r.b0;
  let r_expected = { r_expected with b0 = next_r.b0 } in
  mark_test_run 4;
  let test = eq r r_expected in
  if not test then failwithf "test 4 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a0; b0 } = r in
  let expected_a0 = 100 in
  mark_test_run 5;
  let test = (fun a b -> Int.equal a b) expected_a0 a0 in
  if not test then failwithf "test 5 failed";
  let expected_b0 = 101 in
  mark_test_run 6;
  let test = (fun a b -> Int.equal a b) expected_b0 b0 in
  if not test then failwithf "test 6 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a0; b0 } = r in
  let expected_a0 = 100 in
  mark_test_run 7;
  let test = (fun a b -> Int.equal a b) expected_a0 a0 in
  if not test then failwithf "test 7 failed";
  let expected_b0 = 101 in
  mark_test_run 8;
  let test = (fun a b -> Int.equal a b) expected_b0 b0 in
  if not test then failwithf "test 8 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (********************************)
  (*   t1 = { int; int; float }   *)
  (********************************)
  let r = stack_ { a1 = 0; b1 = 1; c1 = 2. } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a1 *)
  let actual = r.a1 in
  let expected = 0 in
  mark_test_run 9;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 9 failed";
  (* Paths of depth 1 *)
  (* .b1 *)
  let actual = r.b1 in
  let expected = 1 in
  mark_test_run 10;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 10 failed";
  (* Paths of depth 1 *)
  (* .c1 *)
  let actual = r.c1 in
  let expected = 2. in
  mark_test_run 11;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 11 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a1 = a11; b1 = b11; c1 = c11 } { a1 = a12; b1 = b12; c1 = c12 } -> (fun a b -> Int.equal a b) a11 a12 && (fun a b -> Int.equal a b) b11 b12 && (fun a b -> Float.equal (globalize a) (globalize b)) c11 c12) in
  let next_r = stack_ { a1 = 100; b1 = 101; c1 = 102. } in
  let r_expected = stack_ { a1 = 0; b1 = 1; c1 = 2. } in
  (* .a1 *)
  r.a1 <- next_r.a1;
  let r_expected = { r_expected with a1 = next_r.a1 } in
  mark_test_run 12;
  let test = eq r r_expected in
  if not test then failwithf "test 12 failed";
  Gc.compact ();
  (* .b1 *)
  r.b1 <- next_r.b1;
  let r_expected = { r_expected with b1 = next_r.b1 } in
  mark_test_run 13;
  let test = eq r r_expected in
  if not test then failwithf "test 13 failed";
  Gc.compact ();
  (* .c1 *)
  r.c1 <- next_r.c1;
  let r_expected = { r_expected with c1 = next_r.c1 } in
  mark_test_run 14;
  let test = eq r r_expected in
  if not test then failwithf "test 14 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a1; b1; c1 } = r in
  let expected_a1 = 100 in
  mark_test_run 15;
  let test = (fun a b -> Int.equal a b) expected_a1 a1 in
  if not test then failwithf "test 15 failed";
  let expected_b1 = 101 in
  mark_test_run 16;
  let test = (fun a b -> Int.equal a b) expected_b1 b1 in
  if not test then failwithf "test 16 failed";
  let expected_c1 = 102. in
  mark_test_run 17;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_c1 c1 in
  if not test then failwithf "test 17 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a1; b1; c1 } = r in
  let expected_a1 = 100 in
  mark_test_run 18;
  let test = (fun a b -> Int.equal a b) expected_a1 a1 in
  if not test then failwithf "test 18 failed";
  let expected_b1 = 101 in
  mark_test_run 19;
  let test = (fun a b -> Int.equal a b) expected_b1 b1 in
  if not test then failwithf "test 19 failed";
  let expected_c1 = 102. in
  mark_test_run 20;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_c1 c1 in
  if not test then failwithf "test 20 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (****************************)
  (*   t2 = { int; int32# }   *)
  (****************************)
  let r = stack_ { a2 = 0; b2 = #1l } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a2 *)
  let actual = r.a2 in
  let expected = 0 in
  mark_test_run 21;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 21 failed";
  (* Paths of depth 1 *)
  (* .b2 *)
  let actual = r.b2 in
  let expected = #1l in
  mark_test_run 22;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 22 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a2 = a21; b2 = b21 } { a2 = a22; b2 = b22 } -> (fun a b -> Int.equal a b) a21 a22 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b21 b22) in
  let next_r = stack_ { a2 = 100; b2 = #101l } in
  let r_expected = stack_ { a2 = 0; b2 = #1l } in
  (* .a2 *)
  r.a2 <- next_r.a2;
  let r_expected = { r_expected with a2 = next_r.a2 } in
  mark_test_run 23;
  let test = eq r r_expected in
  if not test then failwithf "test 23 failed";
  Gc.compact ();
  (* .b2 *)
  r.b2 <- next_r.b2;
  let r_expected = { r_expected with b2 = next_r.b2 } in
  mark_test_run 24;
  let test = eq r r_expected in
  if not test then failwithf "test 24 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a2; b2 } = r in
  let expected_a2 = 100 in
  mark_test_run 25;
  let test = (fun a b -> Int.equal a b) expected_a2 a2 in
  if not test then failwithf "test 25 failed";
  let expected_b2 = #101l in
  mark_test_run 26;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_b2 b2 in
  if not test then failwithf "test 26 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a2; b2 } = r in
  let expected_a2 = 100 in
  mark_test_run 27;
  let test = (fun a b -> Int.equal a b) expected_a2 a2 in
  if not test then failwithf "test 27 failed";
  let expected_b2 = #101l in
  mark_test_run 28;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_b2 b2 in
  if not test then failwithf "test 28 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (**************************************)
  (*   t4 = { int; #{ int; int32# } }   *)
  (**************************************)
  let r = stack_ { a4 = 0; b4 = #{ a3 = 1; b3 = #2l } } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a4 *)
  let actual = r.a4 in
  let expected = 0 in
  mark_test_run 29;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 29 failed";
  (* Paths of depth 1 *)
  (* .b4 *)
  let actual = r.b4 in
  let expected = #{ a3 = 1; b3 = #2l } in
  mark_test_run 30;
  let test = (fun #{ a3 = a31; b3 = b31 } #{ a3 = a32; b3 = b32 } -> (fun a b -> Int.equal a b) a31 a32 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b31 b32) actual expected in
  if not test then failwithf "test 30 failed";
  (* Paths of depth 2 *)
  (* .b4.#a3 *)
  let actual = r.b4.#a3 in
  let expected = 1 in
  mark_test_run 31;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 31 failed";
  (* .b4.#b3 *)
  let actual = r.b4.#b3 in
  let expected = #2l in
  mark_test_run 32;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 32 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a4 = a41; b4 = b41 } { a4 = a42; b4 = b42 } -> (fun a b -> Int.equal a b) a41 a42 && (fun #{ a3 = a31; b3 = b31 } #{ a3 = a32; b3 = b32 } -> (fun a b -> Int.equal a b) a31 a32 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b31 b32) b41 b42) in
  let next_r = stack_ { a4 = 100; b4 = #{ a3 = 101; b3 = #102l } } in
  let r_expected = stack_ { a4 = 0; b4 = #{ a3 = 1; b3 = #2l } } in
  (* .a4 *)
  r.a4 <- next_r.a4;
  let r_expected = { r_expected with a4 = next_r.a4 } in
  mark_test_run 33;
  let test = eq r r_expected in
  if not test then failwithf "test 33 failed";
  Gc.compact ();
  (* .b4 *)
  r.b4 <- next_r.b4;
  let r_expected = { r_expected with b4 = next_r.b4 } in
  mark_test_run 34;
  let test = eq r r_expected in
  if not test then failwithf "test 34 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a4; b4 = #{ a3; b3 } } = r in
  let expected_a4 = 100 in
  mark_test_run 35;
  let test = (fun a b -> Int.equal a b) expected_a4 a4 in
  if not test then failwithf "test 35 failed";
  let expected_a3 = 101 in
  mark_test_run 36;
  let test = (fun a b -> Int.equal a b) expected_a3 a3 in
  if not test then failwithf "test 36 failed";
  let expected_b3 = #102l in
  mark_test_run 37;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_b3 b3 in
  if not test then failwithf "test 37 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a4; b4 } = r in
  let expected_a4 = 100 in
  mark_test_run 38;
  let test = (fun a b -> Int.equal a b) expected_a4 a4 in
  if not test then failwithf "test 38 failed";
  let expected_b4 = #{ a3 = 101; b3 = #102l } in
  mark_test_run 39;
  let test = (fun #{ a3 = a31; b3 = b31 } #{ a3 = a32; b3 = b32 } -> (fun a b -> Int.equal a b) a31 a32 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b31 b32) expected_b4 b4 in
  if not test then failwithf "test 39 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (*********************************)
  (*   t6 = { int; #{ int32# } }   *)
  (*********************************)
  let r = stack_ { a6 = 0; b6 = #{ a5 = #1l } } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a6 *)
  let actual = r.a6 in
  let expected = 0 in
  mark_test_run 40;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 40 failed";
  (* Paths of depth 1 *)
  (* .b6 *)
  let actual = r.b6 in
  let expected = #{ a5 = #1l } in
  mark_test_run 41;
  let test = (fun #{ a5 = a51 } #{ a5 = a52 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a51 a52) actual expected in
  if not test then failwithf "test 41 failed";
  (* Paths of depth 2 *)
  (* .b6.#a5 *)
  let actual = r.b6.#a5 in
  let expected = #1l in
  mark_test_run 42;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 42 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a6 = a61; b6 = b61 } { a6 = a62; b6 = b62 } -> (fun a b -> Int.equal a b) a61 a62 && (fun #{ a5 = a51 } #{ a5 = a52 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a51 a52) b61 b62) in
  let next_r = stack_ { a6 = 100; b6 = #{ a5 = #101l } } in
  let r_expected = stack_ { a6 = 0; b6 = #{ a5 = #1l } } in
  (* .a6 *)
  r.a6 <- next_r.a6;
  let r_expected = { r_expected with a6 = next_r.a6 } in
  mark_test_run 43;
  let test = eq r r_expected in
  if not test then failwithf "test 43 failed";
  Gc.compact ();
  (* .b6 *)
  r.b6 <- next_r.b6;
  let r_expected = { r_expected with b6 = next_r.b6 } in
  mark_test_run 44;
  let test = eq r r_expected in
  if not test then failwithf "test 44 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a6; b6 = #{ a5 } } = r in
  let expected_a6 = 100 in
  mark_test_run 45;
  let test = (fun a b -> Int.equal a b) expected_a6 a6 in
  if not test then failwithf "test 45 failed";
  let expected_a5 = #101l in
  mark_test_run 46;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a5 a5 in
  if not test then failwithf "test 46 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a6; b6 } = r in
  let expected_a6 = 100 in
  mark_test_run 47;
  let test = (fun a b -> Int.equal a b) expected_a6 a6 in
  if not test then failwithf "test 47 failed";
  let expected_b6 = #{ a5 = #101l } in
  mark_test_run 48;
  let test = (fun #{ a5 = a51 } #{ a5 = a52 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a51 a52) expected_b6 b6 in
  if not test then failwithf "test 48 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (***************************************)
  (*   t8 = { int; #{ float; float } }   *)
  (***************************************)
  let r = stack_ { a8 = 0; b8 = #{ a7 = 1.; b7 = 2. } } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a8 *)
  let actual = r.a8 in
  let expected = 0 in
  mark_test_run 49;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 49 failed";
  (* Paths of depth 1 *)
  (* .b8 *)
  let actual = r.b8 in
  let expected = #{ a7 = 1.; b7 = 2. } in
  mark_test_run 50;
  let test = (fun #{ a7 = a71; b7 = b71 } #{ a7 = a72; b7 = b72 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a71 a72 && (fun a b -> Float.equal (globalize a) (globalize b)) b71 b72) actual expected in
  if not test then failwithf "test 50 failed";
  (* Paths of depth 2 *)
  (* .b8.#a7 *)
  let actual = r.b8.#a7 in
  let expected = 1. in
  mark_test_run 51;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 51 failed";
  (* .b8.#b7 *)
  let actual = r.b8.#b7 in
  let expected = 2. in
  mark_test_run 52;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 52 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a8 = a81; b8 = b81 } { a8 = a82; b8 = b82 } -> (fun a b -> Int.equal a b) a81 a82 && (fun #{ a7 = a71; b7 = b71 } #{ a7 = a72; b7 = b72 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a71 a72 && (fun a b -> Float.equal (globalize a) (globalize b)) b71 b72) b81 b82) in
  let next_r = stack_ { a8 = 100; b8 = #{ a7 = 101.; b7 = 102. } } in
  let r_expected = stack_ { a8 = 0; b8 = #{ a7 = 1.; b7 = 2. } } in
  (* .a8 *)
  r.a8 <- next_r.a8;
  let r_expected = { r_expected with a8 = next_r.a8 } in
  mark_test_run 53;
  let test = eq r r_expected in
  if not test then failwithf "test 53 failed";
  Gc.compact ();
  (* .b8 *)
  r.b8 <- next_r.b8;
  let r_expected = { r_expected with b8 = next_r.b8 } in
  mark_test_run 54;
  let test = eq r r_expected in
  if not test then failwithf "test 54 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a8; b8 = #{ a7; b7 } } = r in
  let expected_a8 = 100 in
  mark_test_run 55;
  let test = (fun a b -> Int.equal a b) expected_a8 a8 in
  if not test then failwithf "test 55 failed";
  let expected_a7 = 101. in
  mark_test_run 56;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a7 a7 in
  if not test then failwithf "test 56 failed";
  let expected_b7 = 102. in
  mark_test_run 57;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b7 b7 in
  if not test then failwithf "test 57 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a8; b8 } = r in
  let expected_a8 = 100 in
  mark_test_run 58;
  let test = (fun a b -> Int.equal a b) expected_a8 a8 in
  if not test then failwithf "test 58 failed";
  let expected_b8 = #{ a7 = 101.; b7 = 102. } in
  mark_test_run 59;
  let test = (fun #{ a7 = a71; b7 = b71 } #{ a7 = a72; b7 = b72 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a71 a72 && (fun a b -> Float.equal (globalize a) (globalize b)) b71 b72) expected_b8 b8 in
  if not test then failwithf "test 59 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (***********************************)
  (*   t9 = { int64#; nativeint# }   *)
  (***********************************)
  let r = stack_ { a9 = #0L; b9 = #1n } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a9 *)
  let actual = r.a9 in
  let expected = #0L in
  mark_test_run 60;
  let test = (fun a b -> Int64_u.(equal (add #0L a) (add #0L b))) actual expected in
  if not test then failwithf "test 60 failed";
  (* Paths of depth 1 *)
  (* .b9 *)
  let actual = r.b9 in
  let expected = #1n in
  mark_test_run 61;
  let test = (fun a b -> Nativeint_u.(equal (add #0n a) (add #0n b))) actual expected in
  if not test then failwithf "test 61 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a9 = a91; b9 = b91 } { a9 = a92; b9 = b92 } -> (fun a b -> Int64_u.(equal (add #0L a) (add #0L b))) a91 a92 && (fun a b -> Nativeint_u.(equal (add #0n a) (add #0n b))) b91 b92) in
  let next_r = stack_ { a9 = #100L; b9 = #101n } in
  let r_expected = stack_ { a9 = #0L; b9 = #1n } in
  (* .a9 *)
  r.a9 <- next_r.a9;
  let r_expected = { r_expected with a9 = next_r.a9 } in
  mark_test_run 62;
  let test = eq r r_expected in
  if not test then failwithf "test 62 failed";
  Gc.compact ();
  (* .b9 *)
  r.b9 <- next_r.b9;
  let r_expected = { r_expected with b9 = next_r.b9 } in
  mark_test_run 63;
  let test = eq r r_expected in
  if not test then failwithf "test 63 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a9; b9 } = r in
  let expected_a9 = #100L in
  mark_test_run 64;
  let test = (fun a b -> Int64_u.(equal (add #0L a) (add #0L b))) expected_a9 a9 in
  if not test then failwithf "test 64 failed";
  let expected_b9 = #101n in
  mark_test_run 65;
  let test = (fun a b -> Nativeint_u.(equal (add #0n a) (add #0n b))) expected_b9 b9 in
  if not test then failwithf "test 65 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a9; b9 } = r in
  let expected_a9 = #100L in
  mark_test_run 66;
  let test = (fun a b -> Int64_u.(equal (add #0L a) (add #0L b))) expected_a9 a9 in
  if not test then failwithf "test 66 failed";
  let expected_b9 = #101n in
  mark_test_run 67;
  let test = (fun a b -> Nativeint_u.(equal (add #0n a) (add #0n b))) expected_b9 b9 in
  if not test then failwithf "test 67 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (************************)
  (*   t10 = { int32# }   *)
  (************************)
  let r = stack_ { a10 = #0l } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a10 *)
  let actual = r.a10 in
  let expected = #0l in
  mark_test_run 68;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 68 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a10 = a101 } { a10 = a102 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a101 a102) in
  let next_r = stack_ { a10 = #100l } in
  let r_expected = stack_ { a10 = #0l } in
  (* .a10 *)
  r.a10 <- next_r.a10;
  let r_expected = { r_expected with a10 = next_r.a10 } in
  mark_test_run 69;
  let test = eq r r_expected in
  if not test then failwithf "test 69 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a10 } = r in
  let expected_a10 = #100l in
  mark_test_run 70;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a10 a10 in
  if not test then failwithf "test 70 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a10 } = r in
  let expected_a10 = #100l in
  mark_test_run 71;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a10 a10 in
  if not test then failwithf "test 71 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (*****************************)
  (*   t11 = { int32#; int }   *)
  (*****************************)
  let r = stack_ { a11 = #0l; b11 = 1 } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a11 *)
  let actual = r.a11 in
  let expected = #0l in
  mark_test_run 72;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 72 failed";
  (* Paths of depth 1 *)
  (* .b11 *)
  let actual = r.b11 in
  let expected = 1 in
  mark_test_run 73;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 73 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a11 = a111; b11 = b111 } { a11 = a112; b11 = b112 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a111 a112 && (fun a b -> Int.equal a b) b111 b112) in
  let next_r = stack_ { a11 = #100l; b11 = 101 } in
  let r_expected = stack_ { a11 = #0l; b11 = 1 } in
  (* .a11 *)
  r.a11 <- next_r.a11;
  let r_expected = { r_expected with a11 = next_r.a11 } in
  mark_test_run 74;
  let test = eq r r_expected in
  if not test then failwithf "test 74 failed";
  Gc.compact ();
  (* .b11 *)
  r.b11 <- next_r.b11;
  let r_expected = { r_expected with b11 = next_r.b11 } in
  mark_test_run 75;
  let test = eq r r_expected in
  if not test then failwithf "test 75 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a11; b11 } = r in
  let expected_a11 = #100l in
  mark_test_run 76;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a11 a11 in
  if not test then failwithf "test 76 failed";
  let expected_b11 = 101 in
  mark_test_run 77;
  let test = (fun a b -> Int.equal a b) expected_b11 b11 in
  if not test then failwithf "test 77 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a11; b11 } = r in
  let expected_a11 = #100l in
  mark_test_run 78;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a11 a11 in
  if not test then failwithf "test 78 failed";
  let expected_b11 = 101 in
  mark_test_run 79;
  let test = (fun a b -> Int.equal a b) expected_b11 b11 in
  if not test then failwithf "test 79 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (**********************************)
  (*   t12 = { int32#; int; int }   *)
  (**********************************)
  let r = stack_ { a12 = #0l; b12 = 1; c12 = 2 } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a12 *)
  let actual = r.a12 in
  let expected = #0l in
  mark_test_run 80;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 80 failed";
  (* Paths of depth 1 *)
  (* .b12 *)
  let actual = r.b12 in
  let expected = 1 in
  mark_test_run 81;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 81 failed";
  (* Paths of depth 1 *)
  (* .c12 *)
  let actual = r.c12 in
  let expected = 2 in
  mark_test_run 82;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 82 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a12 = a121; b12 = b121; c12 = c121 } { a12 = a122; b12 = b122; c12 = c122 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a121 a122 && (fun a b -> Int.equal a b) b121 b122 && (fun a b -> Int.equal a b) c121 c122) in
  let next_r = stack_ { a12 = #100l; b12 = 101; c12 = 102 } in
  let r_expected = stack_ { a12 = #0l; b12 = 1; c12 = 2 } in
  (* .a12 *)
  r.a12 <- next_r.a12;
  let r_expected = { r_expected with a12 = next_r.a12 } in
  mark_test_run 83;
  let test = eq r r_expected in
  if not test then failwithf "test 83 failed";
  Gc.compact ();
  (* .b12 *)
  r.b12 <- next_r.b12;
  let r_expected = { r_expected with b12 = next_r.b12 } in
  mark_test_run 84;
  let test = eq r r_expected in
  if not test then failwithf "test 84 failed";
  Gc.compact ();
  (* .c12 *)
  r.c12 <- next_r.c12;
  let r_expected = { r_expected with c12 = next_r.c12 } in
  mark_test_run 85;
  let test = eq r r_expected in
  if not test then failwithf "test 85 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a12; b12; c12 } = r in
  let expected_a12 = #100l in
  mark_test_run 86;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a12 a12 in
  if not test then failwithf "test 86 failed";
  let expected_b12 = 101 in
  mark_test_run 87;
  let test = (fun a b -> Int.equal a b) expected_b12 b12 in
  if not test then failwithf "test 87 failed";
  let expected_c12 = 102 in
  mark_test_run 88;
  let test = (fun a b -> Int.equal a b) expected_c12 c12 in
  if not test then failwithf "test 88 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a12; b12; c12 } = r in
  let expected_a12 = #100l in
  mark_test_run 89;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a12 a12 in
  if not test then failwithf "test 89 failed";
  let expected_b12 = 101 in
  mark_test_run 90;
  let test = (fun a b -> Int.equal a b) expected_b12 b12 in
  if not test then failwithf "test 90 failed";
  let expected_c12 = 102 in
  mark_test_run 91;
  let test = (fun a b -> Int.equal a b) expected_c12 c12 in
  if not test then failwithf "test 91 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (********************************)
  (*   t13 = { int32#; int32# }   *)
  (********************************)
  let r = stack_ { a13 = #0l; b13 = #1l } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a13 *)
  let actual = r.a13 in
  let expected = #0l in
  mark_test_run 92;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 92 failed";
  (* Paths of depth 1 *)
  (* .b13 *)
  let actual = r.b13 in
  let expected = #1l in
  mark_test_run 93;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 93 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a13 = a131; b13 = b131 } { a13 = a132; b13 = b132 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a131 a132 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b131 b132) in
  let next_r = stack_ { a13 = #100l; b13 = #101l } in
  let r_expected = stack_ { a13 = #0l; b13 = #1l } in
  (* .a13 *)
  r.a13 <- next_r.a13;
  let r_expected = { r_expected with a13 = next_r.a13 } in
  mark_test_run 94;
  let test = eq r r_expected in
  if not test then failwithf "test 94 failed";
  Gc.compact ();
  (* .b13 *)
  r.b13 <- next_r.b13;
  let r_expected = { r_expected with b13 = next_r.b13 } in
  mark_test_run 95;
  let test = eq r r_expected in
  if not test then failwithf "test 95 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a13; b13 } = r in
  let expected_a13 = #100l in
  mark_test_run 96;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a13 a13 in
  if not test then failwithf "test 96 failed";
  let expected_b13 = #101l in
  mark_test_run 97;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_b13 b13 in
  if not test then failwithf "test 97 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a13; b13 } = r in
  let expected_a13 = #100l in
  mark_test_run 98;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a13 a13 in
  if not test then failwithf "test 98 failed";
  let expected_b13 = #101l in
  mark_test_run 99;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_b13 b13 in
  if not test then failwithf "test 99 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (*********************************************)
  (*   t15 = { int32#; #{ int32#; int32# } }   *)
  (*********************************************)
  let r = stack_ { a15 = #0l; b15 = #{ a14 = #1l; b14 = #2l } } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a15 *)
  let actual = r.a15 in
  let expected = #0l in
  mark_test_run 100;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 100 failed";
  (* Paths of depth 1 *)
  (* .b15 *)
  let actual = r.b15 in
  let expected = #{ a14 = #1l; b14 = #2l } in
  mark_test_run 101;
  let test = (fun #{ a14 = a141; b14 = b141 } #{ a14 = a142; b14 = b142 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a141 a142 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b141 b142) actual expected in
  if not test then failwithf "test 101 failed";
  (* Paths of depth 2 *)
  (* .b15.#a14 *)
  let actual = r.b15.#a14 in
  let expected = #1l in
  mark_test_run 102;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 102 failed";
  (* .b15.#b14 *)
  let actual = r.b15.#b14 in
  let expected = #2l in
  mark_test_run 103;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 103 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a15 = a151; b15 = b151 } { a15 = a152; b15 = b152 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a151 a152 && (fun #{ a14 = a141; b14 = b141 } #{ a14 = a142; b14 = b142 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a141 a142 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b141 b142) b151 b152) in
  let next_r = stack_ { a15 = #100l; b15 = #{ a14 = #101l; b14 = #102l } } in
  let r_expected = stack_ { a15 = #0l; b15 = #{ a14 = #1l; b14 = #2l } } in
  (* .a15 *)
  r.a15 <- next_r.a15;
  let r_expected = { r_expected with a15 = next_r.a15 } in
  mark_test_run 104;
  let test = eq r r_expected in
  if not test then failwithf "test 104 failed";
  Gc.compact ();
  (* .b15 *)
  r.b15 <- next_r.b15;
  let r_expected = { r_expected with b15 = next_r.b15 } in
  mark_test_run 105;
  let test = eq r r_expected in
  if not test then failwithf "test 105 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a15; b15 = #{ a14; b14 } } = r in
  let expected_a15 = #100l in
  mark_test_run 106;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a15 a15 in
  if not test then failwithf "test 106 failed";
  let expected_a14 = #101l in
  mark_test_run 107;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a14 a14 in
  if not test then failwithf "test 107 failed";
  let expected_b14 = #102l in
  mark_test_run 108;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_b14 b14 in
  if not test then failwithf "test 108 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a15; b15 } = r in
  let expected_a15 = #100l in
  mark_test_run 109;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a15 a15 in
  if not test then failwithf "test 109 failed";
  let expected_b15 = #{ a14 = #101l; b14 = #102l } in
  mark_test_run 110;
  let test = (fun #{ a14 = a141; b14 = b141 } #{ a14 = a142; b14 = b142 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a141 a142 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b141 b142) expected_b15 b15 in
  if not test then failwithf "test 110 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (****************************************)
  (*   t16 = { nativeint#; nativeint# }   *)
  (****************************************)
  let r = stack_ { a16 = #0n; b16 = #1n } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a16 *)
  let actual = r.a16 in
  let expected = #0n in
  mark_test_run 111;
  let test = (fun a b -> Nativeint_u.(equal (add #0n a) (add #0n b))) actual expected in
  if not test then failwithf "test 111 failed";
  (* Paths of depth 1 *)
  (* .b16 *)
  let actual = r.b16 in
  let expected = #1n in
  mark_test_run 112;
  let test = (fun a b -> Nativeint_u.(equal (add #0n a) (add #0n b))) actual expected in
  if not test then failwithf "test 112 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a16 = a161; b16 = b161 } { a16 = a162; b16 = b162 } -> (fun a b -> Nativeint_u.(equal (add #0n a) (add #0n b))) a161 a162 && (fun a b -> Nativeint_u.(equal (add #0n a) (add #0n b))) b161 b162) in
  let next_r = stack_ { a16 = #100n; b16 = #101n } in
  let r_expected = stack_ { a16 = #0n; b16 = #1n } in
  (* .a16 *)
  r.a16 <- next_r.a16;
  let r_expected = { r_expected with a16 = next_r.a16 } in
  mark_test_run 113;
  let test = eq r r_expected in
  if not test then failwithf "test 113 failed";
  Gc.compact ();
  (* .b16 *)
  r.b16 <- next_r.b16;
  let r_expected = { r_expected with b16 = next_r.b16 } in
  mark_test_run 114;
  let test = eq r r_expected in
  if not test then failwithf "test 114 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a16; b16 } = r in
  let expected_a16 = #100n in
  mark_test_run 115;
  let test = (fun a b -> Nativeint_u.(equal (add #0n a) (add #0n b))) expected_a16 a16 in
  if not test then failwithf "test 115 failed";
  let expected_b16 = #101n in
  mark_test_run 116;
  let test = (fun a b -> Nativeint_u.(equal (add #0n a) (add #0n b))) expected_b16 b16 in
  if not test then failwithf "test 116 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a16; b16 } = r in
  let expected_a16 = #100n in
  mark_test_run 117;
  let test = (fun a b -> Nativeint_u.(equal (add #0n a) (add #0n b))) expected_a16 a16 in
  if not test then failwithf "test 117 failed";
  let expected_b16 = #101n in
  mark_test_run 118;
  let test = (fun a b -> Nativeint_u.(equal (add #0n a) (add #0n b))) expected_b16 b16 in
  if not test then failwithf "test 118 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (***********************)
  (*   t17 = { float }   *)
  (***********************)
  let r = stack_ { a17 = 0. } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a17 *)
  let actual = r.a17 in
  let expected = 0. in
  mark_test_run 119;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 119 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a17 = a171 } { a17 = a172 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a171 a172) in
  let next_r = stack_ { a17 = 100. } in
  let r_expected = stack_ { a17 = 0. } in
  (* .a17 *)
  r.a17 <- next_r.a17;
  let r_expected = { r_expected with a17 = next_r.a17 } in
  mark_test_run 120;
  let test = eq r r_expected in
  if not test then failwithf "test 120 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a17 } = r in
  let expected_a17 = 100. in
  mark_test_run 121;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a17 a17 in
  if not test then failwithf "test 121 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a17 } = r in
  let expected_a17 = 100. in
  mark_test_run 122;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a17 a17 in
  if not test then failwithf "test 122 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (****************************)
  (*   t18 = { float; int }   *)
  (****************************)
  let r = stack_ { a18 = 0.; b18 = 1 } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a18 *)
  let actual = r.a18 in
  let expected = 0. in
  mark_test_run 123;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 123 failed";
  (* Paths of depth 1 *)
  (* .b18 *)
  let actual = r.b18 in
  let expected = 1 in
  mark_test_run 124;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 124 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a18 = a181; b18 = b181 } { a18 = a182; b18 = b182 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a181 a182 && (fun a b -> Int.equal a b) b181 b182) in
  let next_r = stack_ { a18 = 100.; b18 = 101 } in
  let r_expected = stack_ { a18 = 0.; b18 = 1 } in
  (* .a18 *)
  r.a18 <- next_r.a18;
  let r_expected = { r_expected with a18 = next_r.a18 } in
  mark_test_run 125;
  let test = eq r r_expected in
  if not test then failwithf "test 125 failed";
  Gc.compact ();
  (* .b18 *)
  r.b18 <- next_r.b18;
  let r_expected = { r_expected with b18 = next_r.b18 } in
  mark_test_run 126;
  let test = eq r r_expected in
  if not test then failwithf "test 126 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a18; b18 } = r in
  let expected_a18 = 100. in
  mark_test_run 127;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a18 a18 in
  if not test then failwithf "test 127 failed";
  let expected_b18 = 101 in
  mark_test_run 128;
  let test = (fun a b -> Int.equal a b) expected_b18 b18 in
  if not test then failwithf "test 128 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a18; b18 } = r in
  let expected_a18 = 100. in
  mark_test_run 129;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a18 a18 in
  if not test then failwithf "test 129 failed";
  let expected_b18 = 101 in
  mark_test_run 130;
  let test = (fun a b -> Int.equal a b) expected_b18 b18 in
  if not test then failwithf "test 130 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (*********************************)
  (*   t19 = { float; int; int }   *)
  (*********************************)
  let r = stack_ { a19 = 0.; b19 = 1; c19 = 2 } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a19 *)
  let actual = r.a19 in
  let expected = 0. in
  mark_test_run 131;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 131 failed";
  (* Paths of depth 1 *)
  (* .b19 *)
  let actual = r.b19 in
  let expected = 1 in
  mark_test_run 132;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 132 failed";
  (* Paths of depth 1 *)
  (* .c19 *)
  let actual = r.c19 in
  let expected = 2 in
  mark_test_run 133;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 133 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a19 = a191; b19 = b191; c19 = c191 } { a19 = a192; b19 = b192; c19 = c192 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a191 a192 && (fun a b -> Int.equal a b) b191 b192 && (fun a b -> Int.equal a b) c191 c192) in
  let next_r = stack_ { a19 = 100.; b19 = 101; c19 = 102 } in
  let r_expected = stack_ { a19 = 0.; b19 = 1; c19 = 2 } in
  (* .a19 *)
  r.a19 <- next_r.a19;
  let r_expected = { r_expected with a19 = next_r.a19 } in
  mark_test_run 134;
  let test = eq r r_expected in
  if not test then failwithf "test 134 failed";
  Gc.compact ();
  (* .b19 *)
  r.b19 <- next_r.b19;
  let r_expected = { r_expected with b19 = next_r.b19 } in
  mark_test_run 135;
  let test = eq r r_expected in
  if not test then failwithf "test 135 failed";
  Gc.compact ();
  (* .c19 *)
  r.c19 <- next_r.c19;
  let r_expected = { r_expected with c19 = next_r.c19 } in
  mark_test_run 136;
  let test = eq r r_expected in
  if not test then failwithf "test 136 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a19; b19; c19 } = r in
  let expected_a19 = 100. in
  mark_test_run 137;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a19 a19 in
  if not test then failwithf "test 137 failed";
  let expected_b19 = 101 in
  mark_test_run 138;
  let test = (fun a b -> Int.equal a b) expected_b19 b19 in
  if not test then failwithf "test 138 failed";
  let expected_c19 = 102 in
  mark_test_run 139;
  let test = (fun a b -> Int.equal a b) expected_c19 c19 in
  if not test then failwithf "test 139 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a19; b19; c19 } = r in
  let expected_a19 = 100. in
  mark_test_run 140;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a19 a19 in
  if not test then failwithf "test 140 failed";
  let expected_b19 = 101 in
  mark_test_run 141;
  let test = (fun a b -> Int.equal a b) expected_b19 b19 in
  if not test then failwithf "test 141 failed";
  let expected_c19 = 102 in
  mark_test_run 142;
  let test = (fun a b -> Int.equal a b) expected_c19 c19 in
  if not test then failwithf "test 142 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (***********************************)
  (*   t20 = { float; int; float }   *)
  (***********************************)
  let r = stack_ { a20 = 0.; b20 = 1; c20 = 2. } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a20 *)
  let actual = r.a20 in
  let expected = 0. in
  mark_test_run 143;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 143 failed";
  (* Paths of depth 1 *)
  (* .b20 *)
  let actual = r.b20 in
  let expected = 1 in
  mark_test_run 144;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 144 failed";
  (* Paths of depth 1 *)
  (* .c20 *)
  let actual = r.c20 in
  let expected = 2. in
  mark_test_run 145;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 145 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a20 = a201; b20 = b201; c20 = c201 } { a20 = a202; b20 = b202; c20 = c202 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a201 a202 && (fun a b -> Int.equal a b) b201 b202 && (fun a b -> Float.equal (globalize a) (globalize b)) c201 c202) in
  let next_r = stack_ { a20 = 100.; b20 = 101; c20 = 102. } in
  let r_expected = stack_ { a20 = 0.; b20 = 1; c20 = 2. } in
  (* .a20 *)
  r.a20 <- next_r.a20;
  let r_expected = { r_expected with a20 = next_r.a20 } in
  mark_test_run 146;
  let test = eq r r_expected in
  if not test then failwithf "test 146 failed";
  Gc.compact ();
  (* .b20 *)
  r.b20 <- next_r.b20;
  let r_expected = { r_expected with b20 = next_r.b20 } in
  mark_test_run 147;
  let test = eq r r_expected in
  if not test then failwithf "test 147 failed";
  Gc.compact ();
  (* .c20 *)
  r.c20 <- next_r.c20;
  let r_expected = { r_expected with c20 = next_r.c20 } in
  mark_test_run 148;
  let test = eq r r_expected in
  if not test then failwithf "test 148 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a20; b20; c20 } = r in
  let expected_a20 = 100. in
  mark_test_run 149;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a20 a20 in
  if not test then failwithf "test 149 failed";
  let expected_b20 = 101 in
  mark_test_run 150;
  let test = (fun a b -> Int.equal a b) expected_b20 b20 in
  if not test then failwithf "test 150 failed";
  let expected_c20 = 102. in
  mark_test_run 151;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_c20 c20 in
  if not test then failwithf "test 151 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a20; b20; c20 } = r in
  let expected_a20 = 100. in
  mark_test_run 152;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a20 a20 in
  if not test then failwithf "test 152 failed";
  let expected_b20 = 101 in
  mark_test_run 153;
  let test = (fun a b -> Int.equal a b) expected_b20 b20 in
  if not test then failwithf "test 153 failed";
  let expected_c20 = 102. in
  mark_test_run 154;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_c20 c20 in
  if not test then failwithf "test 154 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (*******************************)
  (*   t21 = { float; int32# }   *)
  (*******************************)
  let r = stack_ { a21 = 0.; b21 = #1l } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a21 *)
  let actual = r.a21 in
  let expected = 0. in
  mark_test_run 155;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 155 failed";
  (* Paths of depth 1 *)
  (* .b21 *)
  let actual = r.b21 in
  let expected = #1l in
  mark_test_run 156;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 156 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a21 = a211; b21 = b211 } { a21 = a212; b21 = b212 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a211 a212 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b211 b212) in
  let next_r = stack_ { a21 = 100.; b21 = #101l } in
  let r_expected = stack_ { a21 = 0.; b21 = #1l } in
  (* .a21 *)
  r.a21 <- next_r.a21;
  let r_expected = { r_expected with a21 = next_r.a21 } in
  mark_test_run 157;
  let test = eq r r_expected in
  if not test then failwithf "test 157 failed";
  Gc.compact ();
  (* .b21 *)
  r.b21 <- next_r.b21;
  let r_expected = { r_expected with b21 = next_r.b21 } in
  mark_test_run 158;
  let test = eq r r_expected in
  if not test then failwithf "test 158 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a21; b21 } = r in
  let expected_a21 = 100. in
  mark_test_run 159;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a21 a21 in
  if not test then failwithf "test 159 failed";
  let expected_b21 = #101l in
  mark_test_run 160;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_b21 b21 in
  if not test then failwithf "test 160 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a21; b21 } = r in
  let expected_a21 = 100. in
  mark_test_run 161;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a21 a21 in
  if not test then failwithf "test 161 failed";
  let expected_b21 = #101l in
  mark_test_run 162;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_b21 b21 in
  if not test then failwithf "test 162 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (*************************************)
  (*   t22 = { float; float; float }   *)
  (*************************************)
  let r = stack_ { a22 = 0.; b22 = 1.; c22 = 2. } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a22 *)
  let actual = r.a22 in
  let expected = 0. in
  mark_test_run 163;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 163 failed";
  (* Paths of depth 1 *)
  (* .b22 *)
  let actual = r.b22 in
  let expected = 1. in
  mark_test_run 164;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 164 failed";
  (* Paths of depth 1 *)
  (* .c22 *)
  let actual = r.c22 in
  let expected = 2. in
  mark_test_run 165;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 165 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a22 = a221; b22 = b221; c22 = c221 } { a22 = a222; b22 = b222; c22 = c222 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a221 a222 && (fun a b -> Float.equal (globalize a) (globalize b)) b221 b222 && (fun a b -> Float.equal (globalize a) (globalize b)) c221 c222) in
  let next_r = stack_ { a22 = 100.; b22 = 101.; c22 = 102. } in
  let r_expected = stack_ { a22 = 0.; b22 = 1.; c22 = 2. } in
  (* .a22 *)
  r.a22 <- next_r.a22;
  let r_expected = { r_expected with a22 = next_r.a22 } in
  mark_test_run 166;
  let test = eq r r_expected in
  if not test then failwithf "test 166 failed";
  Gc.compact ();
  (* .b22 *)
  r.b22 <- next_r.b22;
  let r_expected = { r_expected with b22 = next_r.b22 } in
  mark_test_run 167;
  let test = eq r r_expected in
  if not test then failwithf "test 167 failed";
  Gc.compact ();
  (* .c22 *)
  r.c22 <- next_r.c22;
  let r_expected = { r_expected with c22 = next_r.c22 } in
  mark_test_run 168;
  let test = eq r r_expected in
  if not test then failwithf "test 168 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a22; b22; c22 } = r in
  let expected_a22 = 100. in
  mark_test_run 169;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a22 a22 in
  if not test then failwithf "test 169 failed";
  let expected_b22 = 101. in
  mark_test_run 170;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b22 b22 in
  if not test then failwithf "test 170 failed";
  let expected_c22 = 102. in
  mark_test_run 171;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_c22 c22 in
  if not test then failwithf "test 171 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a22; b22; c22 } = r in
  let expected_a22 = 100. in
  mark_test_run 172;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a22 a22 in
  if not test then failwithf "test 172 failed";
  let expected_b22 = 101. in
  mark_test_run 173;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b22 b22 in
  if not test then failwithf "test 173 failed";
  let expected_c22 = 102. in
  mark_test_run 174;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_c22 c22 in
  if not test then failwithf "test 174 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (********************************)
  (*   t23 = { string; string }   *)
  (********************************)
  let r = stack_ { a23 = "0"; b23 = "1" } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a23 *)
  let actual = r.a23 in
  let expected = "0" in
  mark_test_run 175;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 175 failed";
  (* Paths of depth 1 *)
  (* .b23 *)
  let actual = r.b23 in
  let expected = "1" in
  mark_test_run 176;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 176 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a23 = a231; b23 = b231 } { a23 = a232; b23 = b232 } -> (fun a b -> String.equal (globalize a) (globalize b)) a231 a232 && (fun a b -> String.equal (globalize a) (globalize b)) b231 b232) in
  let next_r = stack_ { a23 = "100"; b23 = "101" } in
  let r_expected = stack_ { a23 = "0"; b23 = "1" } in
  (* .a23 *)
  r.a23 <- next_r.a23;
  let r_expected = { r_expected with a23 = next_r.a23 } in
  mark_test_run 177;
  let test = eq r r_expected in
  if not test then failwithf "test 177 failed";
  Gc.compact ();
  (* .b23 *)
  r.b23 <- next_r.b23;
  let r_expected = { r_expected with b23 = next_r.b23 } in
  mark_test_run 178;
  let test = eq r r_expected in
  if not test then failwithf "test 178 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a23; b23 } = r in
  let expected_a23 = "100" in
  mark_test_run 179;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_a23 a23 in
  if not test then failwithf "test 179 failed";
  let expected_b23 = "101" in
  mark_test_run 180;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_b23 b23 in
  if not test then failwithf "test 180 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a23; b23 } = r in
  let expected_a23 = "100" in
  mark_test_run 181;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_a23 a23 in
  if not test then failwithf "test 181 failed";
  let expected_b23 = "101" in
  mark_test_run 182;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_b23 b23 in
  if not test then failwithf "test 182 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (****************************************)
  (*   t24 = { string; string; string }   *)
  (****************************************)
  let r = stack_ { a24 = "0"; b24 = "1"; c24 = "2" } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a24 *)
  let actual = r.a24 in
  let expected = "0" in
  mark_test_run 183;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 183 failed";
  (* Paths of depth 1 *)
  (* .b24 *)
  let actual = r.b24 in
  let expected = "1" in
  mark_test_run 184;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 184 failed";
  (* Paths of depth 1 *)
  (* .c24 *)
  let actual = r.c24 in
  let expected = "2" in
  mark_test_run 185;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 185 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a24 = a241; b24 = b241; c24 = c241 } { a24 = a242; b24 = b242; c24 = c242 } -> (fun a b -> String.equal (globalize a) (globalize b)) a241 a242 && (fun a b -> String.equal (globalize a) (globalize b)) b241 b242 && (fun a b -> String.equal (globalize a) (globalize b)) c241 c242) in
  let next_r = stack_ { a24 = "100"; b24 = "101"; c24 = "102" } in
  let r_expected = stack_ { a24 = "0"; b24 = "1"; c24 = "2" } in
  (* .a24 *)
  r.a24 <- next_r.a24;
  let r_expected = { r_expected with a24 = next_r.a24 } in
  mark_test_run 186;
  let test = eq r r_expected in
  if not test then failwithf "test 186 failed";
  Gc.compact ();
  (* .b24 *)
  r.b24 <- next_r.b24;
  let r_expected = { r_expected with b24 = next_r.b24 } in
  mark_test_run 187;
  let test = eq r r_expected in
  if not test then failwithf "test 187 failed";
  Gc.compact ();
  (* .c24 *)
  r.c24 <- next_r.c24;
  let r_expected = { r_expected with c24 = next_r.c24 } in
  mark_test_run 188;
  let test = eq r r_expected in
  if not test then failwithf "test 188 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a24; b24; c24 } = r in
  let expected_a24 = "100" in
  mark_test_run 189;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_a24 a24 in
  if not test then failwithf "test 189 failed";
  let expected_b24 = "101" in
  mark_test_run 190;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_b24 b24 in
  if not test then failwithf "test 190 failed";
  let expected_c24 = "102" in
  mark_test_run 191;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_c24 c24 in
  if not test then failwithf "test 191 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a24; b24; c24 } = r in
  let expected_a24 = "100" in
  mark_test_run 192;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_a24 a24 in
  if not test then failwithf "test 192 failed";
  let expected_b24 = "101" in
  mark_test_run 193;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_b24 b24 in
  if not test then failwithf "test 193 failed";
  let expected_c24 = "102" in
  mark_test_run 194;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_c24 c24 in
  if not test then failwithf "test 194 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (**************************)
  (*   t26 = { #{ int } }   *)
  (**************************)
  let r = stack_ { a26 = #{ a25 = 0 } } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a26 *)
  let actual = r.a26 in
  let expected = #{ a25 = 0 } in
  mark_test_run 195;
  let test = (fun #{ a25 = a251 } #{ a25 = a252 } -> (fun a b -> Int.equal a b) a251 a252) actual expected in
  if not test then failwithf "test 195 failed";
  (* Paths of depth 2 *)
  (* .a26.#a25 *)
  let actual = r.a26.#a25 in
  let expected = 0 in
  mark_test_run 196;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 196 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a26 = a261 } { a26 = a262 } -> (fun #{ a25 = a251 } #{ a25 = a252 } -> (fun a b -> Int.equal a b) a251 a252) a261 a262) in
  let next_r = stack_ { a26 = #{ a25 = 100 } } in
  let r_expected = stack_ { a26 = #{ a25 = 0 } } in
  (* .a26 *)
  r.a26 <- next_r.a26;
  let r_expected = { r_expected with a26 = next_r.a26 } in
  mark_test_run 197;
  let test = eq r r_expected in
  if not test then failwithf "test 197 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a26 = #{ a25 } } = r in
  let expected_a25 = 100 in
  mark_test_run 198;
  let test = (fun a b -> Int.equal a b) expected_a25 a25 in
  if not test then failwithf "test 198 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a26 } = r in
  let expected_a26 = #{ a25 = 100 } in
  mark_test_run 199;
  let test = (fun #{ a25 = a251 } #{ a25 = a252 } -> (fun a b -> Int.equal a b) a251 a252) expected_a26 a26 in
  if not test then failwithf "test 199 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (*******************************)
  (*   t27 = { #{ int }; int }   *)
  (*******************************)
  let r = stack_ { a27 = #{ a25 = 0 }; b27 = 1 } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a27 *)
  let actual = r.a27 in
  let expected = #{ a25 = 0 } in
  mark_test_run 200;
  let test = (fun #{ a25 = a251 } #{ a25 = a252 } -> (fun a b -> Int.equal a b) a251 a252) actual expected in
  if not test then failwithf "test 200 failed";
  (* Paths of depth 2 *)
  (* .a27.#a25 *)
  let actual = r.a27.#a25 in
  let expected = 0 in
  mark_test_run 201;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 201 failed";
  (* Paths of depth 1 *)
  (* .b27 *)
  let actual = r.b27 in
  let expected = 1 in
  mark_test_run 202;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 202 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a27 = a271; b27 = b271 } { a27 = a272; b27 = b272 } -> (fun #{ a25 = a251 } #{ a25 = a252 } -> (fun a b -> Int.equal a b) a251 a252) a271 a272 && (fun a b -> Int.equal a b) b271 b272) in
  let next_r = stack_ { a27 = #{ a25 = 100 }; b27 = 101 } in
  let r_expected = stack_ { a27 = #{ a25 = 0 }; b27 = 1 } in
  (* .a27 *)
  r.a27 <- next_r.a27;
  let r_expected = { r_expected with a27 = next_r.a27 } in
  mark_test_run 203;
  let test = eq r r_expected in
  if not test then failwithf "test 203 failed";
  Gc.compact ();
  (* .b27 *)
  r.b27 <- next_r.b27;
  let r_expected = { r_expected with b27 = next_r.b27 } in
  mark_test_run 204;
  let test = eq r r_expected in
  if not test then failwithf "test 204 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a27 = #{ a25 }; b27 } = r in
  let expected_a25 = 100 in
  mark_test_run 205;
  let test = (fun a b -> Int.equal a b) expected_a25 a25 in
  if not test then failwithf "test 205 failed";
  let expected_b27 = 101 in
  mark_test_run 206;
  let test = (fun a b -> Int.equal a b) expected_b27 b27 in
  if not test then failwithf "test 206 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a27; b27 } = r in
  let expected_a27 = #{ a25 = 100 } in
  mark_test_run 207;
  let test = (fun #{ a25 = a251 } #{ a25 = a252 } -> (fun a b -> Int.equal a b) a251 a252) expected_a27 a27 in
  if not test then failwithf "test 207 failed";
  let expected_b27 = 101 in
  mark_test_run 208;
  let test = (fun a b -> Int.equal a b) expected_b27 b27 in
  if not test then failwithf "test 208 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (************************************)
  (*   t29 = { #{ int; int }; int }   *)
  (************************************)
  let r = stack_ { a29 = #{ a28 = 0; b28 = 1 }; b29 = 2 } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a29 *)
  let actual = r.a29 in
  let expected = #{ a28 = 0; b28 = 1 } in
  mark_test_run 209;
  let test = (fun #{ a28 = a281; b28 = b281 } #{ a28 = a282; b28 = b282 } -> (fun a b -> Int.equal a b) a281 a282 && (fun a b -> Int.equal a b) b281 b282) actual expected in
  if not test then failwithf "test 209 failed";
  (* Paths of depth 2 *)
  (* .a29.#a28 *)
  let actual = r.a29.#a28 in
  let expected = 0 in
  mark_test_run 210;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 210 failed";
  (* .a29.#b28 *)
  let actual = r.a29.#b28 in
  let expected = 1 in
  mark_test_run 211;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 211 failed";
  (* Paths of depth 1 *)
  (* .b29 *)
  let actual = r.b29 in
  let expected = 2 in
  mark_test_run 212;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 212 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a29 = a291; b29 = b291 } { a29 = a292; b29 = b292 } -> (fun #{ a28 = a281; b28 = b281 } #{ a28 = a282; b28 = b282 } -> (fun a b -> Int.equal a b) a281 a282 && (fun a b -> Int.equal a b) b281 b282) a291 a292 && (fun a b -> Int.equal a b) b291 b292) in
  let next_r = stack_ { a29 = #{ a28 = 100; b28 = 101 }; b29 = 102 } in
  let r_expected = stack_ { a29 = #{ a28 = 0; b28 = 1 }; b29 = 2 } in
  (* .a29 *)
  r.a29 <- next_r.a29;
  let r_expected = { r_expected with a29 = next_r.a29 } in
  mark_test_run 213;
  let test = eq r r_expected in
  if not test then failwithf "test 213 failed";
  Gc.compact ();
  (* .b29 *)
  r.b29 <- next_r.b29;
  let r_expected = { r_expected with b29 = next_r.b29 } in
  mark_test_run 214;
  let test = eq r r_expected in
  if not test then failwithf "test 214 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a29 = #{ a28; b28 }; b29 } = r in
  let expected_a28 = 100 in
  mark_test_run 215;
  let test = (fun a b -> Int.equal a b) expected_a28 a28 in
  if not test then failwithf "test 215 failed";
  let expected_b28 = 101 in
  mark_test_run 216;
  let test = (fun a b -> Int.equal a b) expected_b28 b28 in
  if not test then failwithf "test 216 failed";
  let expected_b29 = 102 in
  mark_test_run 217;
  let test = (fun a b -> Int.equal a b) expected_b29 b29 in
  if not test then failwithf "test 217 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a29; b29 } = r in
  let expected_a29 = #{ a28 = 100; b28 = 101 } in
  mark_test_run 218;
  let test = (fun #{ a28 = a281; b28 = b281 } #{ a28 = a282; b28 = b282 } -> (fun a b -> Int.equal a b) a281 a282 && (fun a b -> Int.equal a b) b281 b282) expected_a29 a29 in
  if not test then failwithf "test 218 failed";
  let expected_b29 = 102 in
  mark_test_run 219;
  let test = (fun a b -> Int.equal a b) expected_b29 b29 in
  if not test then failwithf "test 219 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (*********************************)
  (*   t31 = { #{ int; float } }   *)
  (*********************************)
  let r = stack_ { a31 = #{ a30 = 0; b30 = 1. } } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a31 *)
  let actual = r.a31 in
  let expected = #{ a30 = 0; b30 = 1. } in
  mark_test_run 220;
  let test = (fun #{ a30 = a301; b30 = b301 } #{ a30 = a302; b30 = b302 } -> (fun a b -> Int.equal a b) a301 a302 && (fun a b -> Float.equal (globalize a) (globalize b)) b301 b302) actual expected in
  if not test then failwithf "test 220 failed";
  (* Paths of depth 2 *)
  (* .a31.#a30 *)
  let actual = r.a31.#a30 in
  let expected = 0 in
  mark_test_run 221;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 221 failed";
  (* .a31.#b30 *)
  let actual = r.a31.#b30 in
  let expected = 1. in
  mark_test_run 222;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 222 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a31 = a311 } { a31 = a312 } -> (fun #{ a30 = a301; b30 = b301 } #{ a30 = a302; b30 = b302 } -> (fun a b -> Int.equal a b) a301 a302 && (fun a b -> Float.equal (globalize a) (globalize b)) b301 b302) a311 a312) in
  let next_r = stack_ { a31 = #{ a30 = 100; b30 = 101. } } in
  let r_expected = stack_ { a31 = #{ a30 = 0; b30 = 1. } } in
  (* .a31 *)
  r.a31 <- next_r.a31;
  let r_expected = { r_expected with a31 = next_r.a31 } in
  mark_test_run 223;
  let test = eq r r_expected in
  if not test then failwithf "test 223 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a31 = #{ a30; b30 } } = r in
  let expected_a30 = 100 in
  mark_test_run 224;
  let test = (fun a b -> Int.equal a b) expected_a30 a30 in
  if not test then failwithf "test 224 failed";
  let expected_b30 = 101. in
  mark_test_run 225;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b30 b30 in
  if not test then failwithf "test 225 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a31 } = r in
  let expected_a31 = #{ a30 = 100; b30 = 101. } in
  mark_test_run 226;
  let test = (fun #{ a30 = a301; b30 = b301 } #{ a30 = a302; b30 = b302 } -> (fun a b -> Int.equal a b) a301 a302 && (fun a b -> Float.equal (globalize a) (globalize b)) b301 b302) expected_a31 a31 in
  if not test then failwithf "test 226 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (**********************************)
  (*   t32 = { #{ int32# }; int }   *)
  (**********************************)
  let r = stack_ { a32 = #{ a5 = #0l }; b32 = 1 } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a32 *)
  let actual = r.a32 in
  let expected = #{ a5 = #0l } in
  mark_test_run 227;
  let test = (fun #{ a5 = a51 } #{ a5 = a52 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a51 a52) actual expected in
  if not test then failwithf "test 227 failed";
  (* Paths of depth 2 *)
  (* .a32.#a5 *)
  let actual = r.a32.#a5 in
  let expected = #0l in
  mark_test_run 228;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 228 failed";
  (* Paths of depth 1 *)
  (* .b32 *)
  let actual = r.b32 in
  let expected = 1 in
  mark_test_run 229;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 229 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a32 = a321; b32 = b321 } { a32 = a322; b32 = b322 } -> (fun #{ a5 = a51 } #{ a5 = a52 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a51 a52) a321 a322 && (fun a b -> Int.equal a b) b321 b322) in
  let next_r = stack_ { a32 = #{ a5 = #100l }; b32 = 101 } in
  let r_expected = stack_ { a32 = #{ a5 = #0l }; b32 = 1 } in
  (* .a32 *)
  r.a32 <- next_r.a32;
  let r_expected = { r_expected with a32 = next_r.a32 } in
  mark_test_run 230;
  let test = eq r r_expected in
  if not test then failwithf "test 230 failed";
  Gc.compact ();
  (* .b32 *)
  r.b32 <- next_r.b32;
  let r_expected = { r_expected with b32 = next_r.b32 } in
  mark_test_run 231;
  let test = eq r r_expected in
  if not test then failwithf "test 231 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a32 = #{ a5 }; b32 } = r in
  let expected_a5 = #100l in
  mark_test_run 232;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a5 a5 in
  if not test then failwithf "test 232 failed";
  let expected_b32 = 101 in
  mark_test_run 233;
  let test = (fun a b -> Int.equal a b) expected_b32 b32 in
  if not test then failwithf "test 233 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a32; b32 } = r in
  let expected_a32 = #{ a5 = #100l } in
  mark_test_run 234;
  let test = (fun #{ a5 = a51 } #{ a5 = a52 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a51 a52) expected_a32 a32 in
  if not test then failwithf "test 234 failed";
  let expected_b32 = 101 in
  mark_test_run 235;
  let test = (fun a b -> Int.equal a b) expected_b32 b32 in
  if not test then failwithf "test 235 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (*************************************)
  (*   t33 = { #{ int32# }; int32# }   *)
  (*************************************)
  let r = stack_ { a33 = #{ a5 = #0l }; b33 = #1l } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a33 *)
  let actual = r.a33 in
  let expected = #{ a5 = #0l } in
  mark_test_run 236;
  let test = (fun #{ a5 = a51 } #{ a5 = a52 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a51 a52) actual expected in
  if not test then failwithf "test 236 failed";
  (* Paths of depth 2 *)
  (* .a33.#a5 *)
  let actual = r.a33.#a5 in
  let expected = #0l in
  mark_test_run 237;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 237 failed";
  (* Paths of depth 1 *)
  (* .b33 *)
  let actual = r.b33 in
  let expected = #1l in
  mark_test_run 238;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 238 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a33 = a331; b33 = b331 } { a33 = a332; b33 = b332 } -> (fun #{ a5 = a51 } #{ a5 = a52 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a51 a52) a331 a332 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b331 b332) in
  let next_r = stack_ { a33 = #{ a5 = #100l }; b33 = #101l } in
  let r_expected = stack_ { a33 = #{ a5 = #0l }; b33 = #1l } in
  (* .a33 *)
  r.a33 <- next_r.a33;
  let r_expected = { r_expected with a33 = next_r.a33 } in
  mark_test_run 239;
  let test = eq r r_expected in
  if not test then failwithf "test 239 failed";
  Gc.compact ();
  (* .b33 *)
  r.b33 <- next_r.b33;
  let r_expected = { r_expected with b33 = next_r.b33 } in
  mark_test_run 240;
  let test = eq r r_expected in
  if not test then failwithf "test 240 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a33 = #{ a5 }; b33 } = r in
  let expected_a5 = #100l in
  mark_test_run 241;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a5 a5 in
  if not test then failwithf "test 241 failed";
  let expected_b33 = #101l in
  mark_test_run 242;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_b33 b33 in
  if not test then failwithf "test 242 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a33; b33 } = r in
  let expected_a33 = #{ a5 = #100l } in
  mark_test_run 243;
  let test = (fun #{ a5 = a51 } #{ a5 = a52 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a51 a52) expected_a33 a33 in
  if not test then failwithf "test 243 failed";
  let expected_b33 = #101l in
  mark_test_run 244;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_b33 b33 in
  if not test then failwithf "test 244 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (******************************************)
  (*   t34 = { #{ int32#; int32# }; int }   *)
  (******************************************)
  let r = stack_ { a34 = #{ a14 = #0l; b14 = #1l }; b34 = 2 } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a34 *)
  let actual = r.a34 in
  let expected = #{ a14 = #0l; b14 = #1l } in
  mark_test_run 245;
  let test = (fun #{ a14 = a141; b14 = b141 } #{ a14 = a142; b14 = b142 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a141 a142 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b141 b142) actual expected in
  if not test then failwithf "test 245 failed";
  (* Paths of depth 2 *)
  (* .a34.#a14 *)
  let actual = r.a34.#a14 in
  let expected = #0l in
  mark_test_run 246;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 246 failed";
  (* .a34.#b14 *)
  let actual = r.a34.#b14 in
  let expected = #1l in
  mark_test_run 247;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) actual expected in
  if not test then failwithf "test 247 failed";
  (* Paths of depth 1 *)
  (* .b34 *)
  let actual = r.b34 in
  let expected = 2 in
  mark_test_run 248;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 248 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a34 = a341; b34 = b341 } { a34 = a342; b34 = b342 } -> (fun #{ a14 = a141; b14 = b141 } #{ a14 = a142; b14 = b142 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a141 a142 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b141 b142) a341 a342 && (fun a b -> Int.equal a b) b341 b342) in
  let next_r = stack_ { a34 = #{ a14 = #100l; b14 = #101l }; b34 = 102 } in
  let r_expected = stack_ { a34 = #{ a14 = #0l; b14 = #1l }; b34 = 2 } in
  (* .a34 *)
  r.a34 <- next_r.a34;
  let r_expected = { r_expected with a34 = next_r.a34 } in
  mark_test_run 249;
  let test = eq r r_expected in
  if not test then failwithf "test 249 failed";
  Gc.compact ();
  (* .b34 *)
  r.b34 <- next_r.b34;
  let r_expected = { r_expected with b34 = next_r.b34 } in
  mark_test_run 250;
  let test = eq r r_expected in
  if not test then failwithf "test 250 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a34 = #{ a14; b14 }; b34 } = r in
  let expected_a14 = #100l in
  mark_test_run 251;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_a14 a14 in
  if not test then failwithf "test 251 failed";
  let expected_b14 = #101l in
  mark_test_run 252;
  let test = (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) expected_b14 b14 in
  if not test then failwithf "test 252 failed";
  let expected_b34 = 102 in
  mark_test_run 253;
  let test = (fun a b -> Int.equal a b) expected_b34 b34 in
  if not test then failwithf "test 253 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a34; b34 } = r in
  let expected_a34 = #{ a14 = #100l; b14 = #101l } in
  mark_test_run 254;
  let test = (fun #{ a14 = a141; b14 = b141 } #{ a14 = a142; b14 = b142 } -> (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) a141 a142 && (fun a b -> Int32_u.(equal (add #0l a) (add #0l b))) b141 b142) expected_a34 a34 in
  if not test then failwithf "test 254 failed";
  let expected_b34 = 102 in
  mark_test_run 255;
  let test = (fun a b -> Int.equal a b) expected_b34 b34 in
  if not test then failwithf "test 255 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (*********************************)
  (*   t36 = { #{ float }; int }   *)
  (*********************************)
  let r = stack_ { a36 = #{ a35 = 0. }; b36 = 1 } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a36 *)
  let actual = r.a36 in
  let expected = #{ a35 = 0. } in
  mark_test_run 256;
  let test = (fun #{ a35 = a351 } #{ a35 = a352 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a351 a352) actual expected in
  if not test then failwithf "test 256 failed";
  (* Paths of depth 2 *)
  (* .a36.#a35 *)
  let actual = r.a36.#a35 in
  let expected = 0. in
  mark_test_run 257;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 257 failed";
  (* Paths of depth 1 *)
  (* .b36 *)
  let actual = r.b36 in
  let expected = 1 in
  mark_test_run 258;
  let test = (fun a b -> Int.equal a b) actual expected in
  if not test then failwithf "test 258 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a36 = a361; b36 = b361 } { a36 = a362; b36 = b362 } -> (fun #{ a35 = a351 } #{ a35 = a352 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a351 a352) a361 a362 && (fun a b -> Int.equal a b) b361 b362) in
  let next_r = stack_ { a36 = #{ a35 = 100. }; b36 = 101 } in
  let r_expected = stack_ { a36 = #{ a35 = 0. }; b36 = 1 } in
  (* .a36 *)
  r.a36 <- next_r.a36;
  let r_expected = { r_expected with a36 = next_r.a36 } in
  mark_test_run 259;
  let test = eq r r_expected in
  if not test then failwithf "test 259 failed";
  Gc.compact ();
  (* .b36 *)
  r.b36 <- next_r.b36;
  let r_expected = { r_expected with b36 = next_r.b36 } in
  mark_test_run 260;
  let test = eq r r_expected in
  if not test then failwithf "test 260 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a36 = #{ a35 }; b36 } = r in
  let expected_a35 = 100. in
  mark_test_run 261;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a35 a35 in
  if not test then failwithf "test 261 failed";
  let expected_b36 = 101 in
  mark_test_run 262;
  let test = (fun a b -> Int.equal a b) expected_b36 b36 in
  if not test then failwithf "test 262 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a36; b36 } = r in
  let expected_a36 = #{ a35 = 100. } in
  mark_test_run 263;
  let test = (fun #{ a35 = a351 } #{ a35 = a352 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a351 a352) expected_a36 a36 in
  if not test then failwithf "test 263 failed";
  let expected_b36 = 101 in
  mark_test_run 264;
  let test = (fun a b -> Int.equal a b) expected_b36 b36 in
  if not test then failwithf "test 264 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (***********************************)
  (*   t37 = { #{ float }; float }   *)
  (***********************************)
  let r = stack_ { a37 = #{ a35 = 0. }; b37 = 1. } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a37 *)
  let actual = r.a37 in
  let expected = #{ a35 = 0. } in
  mark_test_run 265;
  let test = (fun #{ a35 = a351 } #{ a35 = a352 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a351 a352) actual expected in
  if not test then failwithf "test 265 failed";
  (* Paths of depth 2 *)
  (* .a37.#a35 *)
  let actual = r.a37.#a35 in
  let expected = 0. in
  mark_test_run 266;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 266 failed";
  (* Paths of depth 1 *)
  (* .b37 *)
  let actual = r.b37 in
  let expected = 1. in
  mark_test_run 267;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 267 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a37 = a371; b37 = b371 } { a37 = a372; b37 = b372 } -> (fun #{ a35 = a351 } #{ a35 = a352 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a351 a352) a371 a372 && (fun a b -> Float.equal (globalize a) (globalize b)) b371 b372) in
  let next_r = stack_ { a37 = #{ a35 = 100. }; b37 = 101. } in
  let r_expected = stack_ { a37 = #{ a35 = 0. }; b37 = 1. } in
  (* .a37 *)
  r.a37 <- next_r.a37;
  let r_expected = { r_expected with a37 = next_r.a37 } in
  mark_test_run 268;
  let test = eq r r_expected in
  if not test then failwithf "test 268 failed";
  Gc.compact ();
  (* .b37 *)
  r.b37 <- next_r.b37;
  let r_expected = { r_expected with b37 = next_r.b37 } in
  mark_test_run 269;
  let test = eq r r_expected in
  if not test then failwithf "test 269 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a37 = #{ a35 }; b37 } = r in
  let expected_a35 = 100. in
  mark_test_run 270;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a35 a35 in
  if not test then failwithf "test 270 failed";
  let expected_b37 = 101. in
  mark_test_run 271;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b37 b37 in
  if not test then failwithf "test 271 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a37; b37 } = r in
  let expected_a37 = #{ a35 = 100. } in
  mark_test_run 272;
  let test = (fun #{ a35 = a351 } #{ a35 = a352 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a351 a352) expected_a37 a37 in
  if not test then failwithf "test 272 failed";
  let expected_b37 = 101. in
  mark_test_run 273;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b37 b37 in
  if not test then failwithf "test 273 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (************************************)
  (*   t38 = { #{ float }; float# }   *)
  (************************************)
  let r = stack_ { a38 = #{ a35 = 0. }; b38 = #1. } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a38 *)
  let actual = r.a38 in
  let expected = #{ a35 = 0. } in
  mark_test_run 274;
  let test = (fun #{ a35 = a351 } #{ a35 = a352 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a351 a352) actual expected in
  if not test then failwithf "test 274 failed";
  (* Paths of depth 2 *)
  (* .a38.#a35 *)
  let actual = r.a38.#a35 in
  let expected = 0. in
  mark_test_run 275;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 275 failed";
  (* Paths of depth 1 *)
  (* .b38 *)
  let actual = r.b38 in
  let expected = #1. in
  mark_test_run 276;
  let test = (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) actual expected in
  if not test then failwithf "test 276 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a38 = a381; b38 = b381 } { a38 = a382; b38 = b382 } -> (fun #{ a35 = a351 } #{ a35 = a352 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a351 a352) a381 a382 && (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) b381 b382) in
  let next_r = stack_ { a38 = #{ a35 = 100. }; b38 = #101. } in
  let r_expected = stack_ { a38 = #{ a35 = 0. }; b38 = #1. } in
  (* .a38 *)
  r.a38 <- next_r.a38;
  let r_expected = { r_expected with a38 = next_r.a38 } in
  mark_test_run 277;
  let test = eq r r_expected in
  if not test then failwithf "test 277 failed";
  Gc.compact ();
  (* .b38 *)
  r.b38 <- next_r.b38;
  let r_expected = { r_expected with b38 = next_r.b38 } in
  mark_test_run 278;
  let test = eq r r_expected in
  if not test then failwithf "test 278 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a38 = #{ a35 }; b38 } = r in
  let expected_a35 = 100. in
  mark_test_run 279;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a35 a35 in
  if not test then failwithf "test 279 failed";
  let expected_b38 = #101. in
  mark_test_run 280;
  let test = (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) expected_b38 b38 in
  if not test then failwithf "test 280 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a38; b38 } = r in
  let expected_a38 = #{ a35 = 100. } in
  mark_test_run 281;
  let test = (fun #{ a35 = a351 } #{ a35 = a352 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a351 a352) expected_a38 a38 in
  if not test then failwithf "test 281 failed";
  let expected_b38 = #101. in
  mark_test_run 282;
  let test = (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) expected_b38 b38 in
  if not test then failwithf "test 282 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (***********************************)
  (*   t39 = { #{ float; float } }   *)
  (***********************************)
  let r = stack_ { a39 = #{ a7 = 0.; b7 = 1. } } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a39 *)
  let actual = r.a39 in
  let expected = #{ a7 = 0.; b7 = 1. } in
  mark_test_run 283;
  let test = (fun #{ a7 = a71; b7 = b71 } #{ a7 = a72; b7 = b72 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a71 a72 && (fun a b -> Float.equal (globalize a) (globalize b)) b71 b72) actual expected in
  if not test then failwithf "test 283 failed";
  (* Paths of depth 2 *)
  (* .a39.#a7 *)
  let actual = r.a39.#a7 in
  let expected = 0. in
  mark_test_run 284;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 284 failed";
  (* .a39.#b7 *)
  let actual = r.a39.#b7 in
  let expected = 1. in
  mark_test_run 285;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 285 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a39 = a391 } { a39 = a392 } -> (fun #{ a7 = a71; b7 = b71 } #{ a7 = a72; b7 = b72 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a71 a72 && (fun a b -> Float.equal (globalize a) (globalize b)) b71 b72) a391 a392) in
  let next_r = stack_ { a39 = #{ a7 = 100.; b7 = 101. } } in
  let r_expected = stack_ { a39 = #{ a7 = 0.; b7 = 1. } } in
  (* .a39 *)
  r.a39 <- next_r.a39;
  let r_expected = { r_expected with a39 = next_r.a39 } in
  mark_test_run 286;
  let test = eq r r_expected in
  if not test then failwithf "test 286 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a39 = #{ a7; b7 } } = r in
  let expected_a7 = 100. in
  mark_test_run 287;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a7 a7 in
  if not test then failwithf "test 287 failed";
  let expected_b7 = 101. in
  mark_test_run 288;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b7 b7 in
  if not test then failwithf "test 288 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a39 } = r in
  let expected_a39 = #{ a7 = 100.; b7 = 101. } in
  mark_test_run 289;
  let test = (fun #{ a7 = a71; b7 = b71 } #{ a7 = a72; b7 = b72 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a71 a72 && (fun a b -> Float.equal (globalize a) (globalize b)) b71 b72) expected_a39 a39 in
  if not test then failwithf "test 289 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (******************************************)
  (*   t40 = { #{ float; float }; float }   *)
  (******************************************)
  let r = stack_ { a40 = #{ a7 = 0.; b7 = 1. }; b40 = 2. } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a40 *)
  let actual = r.a40 in
  let expected = #{ a7 = 0.; b7 = 1. } in
  mark_test_run 290;
  let test = (fun #{ a7 = a71; b7 = b71 } #{ a7 = a72; b7 = b72 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a71 a72 && (fun a b -> Float.equal (globalize a) (globalize b)) b71 b72) actual expected in
  if not test then failwithf "test 290 failed";
  (* Paths of depth 2 *)
  (* .a40.#a7 *)
  let actual = r.a40.#a7 in
  let expected = 0. in
  mark_test_run 291;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 291 failed";
  (* .a40.#b7 *)
  let actual = r.a40.#b7 in
  let expected = 1. in
  mark_test_run 292;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 292 failed";
  (* Paths of depth 1 *)
  (* .b40 *)
  let actual = r.b40 in
  let expected = 2. in
  mark_test_run 293;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 293 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a40 = a401; b40 = b401 } { a40 = a402; b40 = b402 } -> (fun #{ a7 = a71; b7 = b71 } #{ a7 = a72; b7 = b72 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a71 a72 && (fun a b -> Float.equal (globalize a) (globalize b)) b71 b72) a401 a402 && (fun a b -> Float.equal (globalize a) (globalize b)) b401 b402) in
  let next_r = stack_ { a40 = #{ a7 = 100.; b7 = 101. }; b40 = 102. } in
  let r_expected = stack_ { a40 = #{ a7 = 0.; b7 = 1. }; b40 = 2. } in
  (* .a40 *)
  r.a40 <- next_r.a40;
  let r_expected = { r_expected with a40 = next_r.a40 } in
  mark_test_run 294;
  let test = eq r r_expected in
  if not test then failwithf "test 294 failed";
  Gc.compact ();
  (* .b40 *)
  r.b40 <- next_r.b40;
  let r_expected = { r_expected with b40 = next_r.b40 } in
  mark_test_run 295;
  let test = eq r r_expected in
  if not test then failwithf "test 295 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a40 = #{ a7; b7 }; b40 } = r in
  let expected_a7 = 100. in
  mark_test_run 296;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a7 a7 in
  if not test then failwithf "test 296 failed";
  let expected_b7 = 101. in
  mark_test_run 297;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b7 b7 in
  if not test then failwithf "test 297 failed";
  let expected_b40 = 102. in
  mark_test_run 298;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b40 b40 in
  if not test then failwithf "test 298 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a40; b40 } = r in
  let expected_a40 = #{ a7 = 100.; b7 = 101. } in
  mark_test_run 299;
  let test = (fun #{ a7 = a71; b7 = b71 } #{ a7 = a72; b7 = b72 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a71 a72 && (fun a b -> Float.equal (globalize a) (globalize b)) b71 b72) expected_a40 a40 in
  if not test then failwithf "test 299 failed";
  let expected_b40 = 102. in
  mark_test_run 300;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b40 b40 in
  if not test then failwithf "test 300 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (*******************************************)
  (*   t42 = { #{ float; float# }; float }   *)
  (*******************************************)
  let r = stack_ { a42 = #{ a41 = 0.; b41 = #1. }; b42 = 2. } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a42 *)
  let actual = r.a42 in
  let expected = #{ a41 = 0.; b41 = #1. } in
  mark_test_run 301;
  let test = (fun #{ a41 = a411; b41 = b411 } #{ a41 = a412; b41 = b412 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a411 a412 && (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) b411 b412) actual expected in
  if not test then failwithf "test 301 failed";
  (* Paths of depth 2 *)
  (* .a42.#a41 *)
  let actual = r.a42.#a41 in
  let expected = 0. in
  mark_test_run 302;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 302 failed";
  (* .a42.#b41 *)
  let actual = r.a42.#b41 in
  let expected = #1. in
  mark_test_run 303;
  let test = (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) actual expected in
  if not test then failwithf "test 303 failed";
  (* Paths of depth 1 *)
  (* .b42 *)
  let actual = r.b42 in
  let expected = 2. in
  mark_test_run 304;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 304 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a42 = a421; b42 = b421 } { a42 = a422; b42 = b422 } -> (fun #{ a41 = a411; b41 = b411 } #{ a41 = a412; b41 = b412 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a411 a412 && (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) b411 b412) a421 a422 && (fun a b -> Float.equal (globalize a) (globalize b)) b421 b422) in
  let next_r = stack_ { a42 = #{ a41 = 100.; b41 = #101. }; b42 = 102. } in
  let r_expected = stack_ { a42 = #{ a41 = 0.; b41 = #1. }; b42 = 2. } in
  (* .a42 *)
  r.a42 <- next_r.a42;
  let r_expected = { r_expected with a42 = next_r.a42 } in
  mark_test_run 305;
  let test = eq r r_expected in
  if not test then failwithf "test 305 failed";
  Gc.compact ();
  (* .b42 *)
  r.b42 <- next_r.b42;
  let r_expected = { r_expected with b42 = next_r.b42 } in
  mark_test_run 306;
  let test = eq r r_expected in
  if not test then failwithf "test 306 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a42 = #{ a41; b41 }; b42 } = r in
  let expected_a41 = 100. in
  mark_test_run 307;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_a41 a41 in
  if not test then failwithf "test 307 failed";
  let expected_b41 = #101. in
  mark_test_run 308;
  let test = (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) expected_b41 b41 in
  if not test then failwithf "test 308 failed";
  let expected_b42 = 102. in
  mark_test_run 309;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b42 b42 in
  if not test then failwithf "test 309 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a42; b42 } = r in
  let expected_a42 = #{ a41 = 100.; b41 = #101. } in
  mark_test_run 310;
  let test = (fun #{ a41 = a411; b41 = b411 } #{ a41 = a412; b41 = b412 } -> (fun a b -> Float.equal (globalize a) (globalize b)) a411 a412 && (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) b411 b412) expected_a42 a42 in
  if not test then failwithf "test 310 failed";
  let expected_b42 = 102. in
  mark_test_run 311;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b42 b42 in
  if not test then failwithf "test 311 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (********************************************)
  (*   t44 = { #{ float#; float }; float# }   *)
  (********************************************)
  let r = stack_ { a44 = #{ a43 = #0.; b43 = 1. }; b44 = #2. } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a44 *)
  let actual = r.a44 in
  let expected = #{ a43 = #0.; b43 = 1. } in
  mark_test_run 312;
  let test = (fun #{ a43 = a431; b43 = b431 } #{ a43 = a432; b43 = b432 } -> (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) a431 a432 && (fun a b -> Float.equal (globalize a) (globalize b)) b431 b432) actual expected in
  if not test then failwithf "test 312 failed";
  (* Paths of depth 2 *)
  (* .a44.#a43 *)
  let actual = r.a44.#a43 in
  let expected = #0. in
  mark_test_run 313;
  let test = (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) actual expected in
  if not test then failwithf "test 313 failed";
  (* .a44.#b43 *)
  let actual = r.a44.#b43 in
  let expected = 1. in
  mark_test_run 314;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 314 failed";
  (* Paths of depth 1 *)
  (* .b44 *)
  let actual = r.b44 in
  let expected = #2. in
  mark_test_run 315;
  let test = (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) actual expected in
  if not test then failwithf "test 315 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a44 = a441; b44 = b441 } { a44 = a442; b44 = b442 } -> (fun #{ a43 = a431; b43 = b431 } #{ a43 = a432; b43 = b432 } -> (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) a431 a432 && (fun a b -> Float.equal (globalize a) (globalize b)) b431 b432) a441 a442 && (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) b441 b442) in
  let next_r = stack_ { a44 = #{ a43 = #100.; b43 = 101. }; b44 = #102. } in
  let r_expected = stack_ { a44 = #{ a43 = #0.; b43 = 1. }; b44 = #2. } in
  (* .a44 *)
  r.a44 <- next_r.a44;
  let r_expected = { r_expected with a44 = next_r.a44 } in
  mark_test_run 316;
  let test = eq r r_expected in
  if not test then failwithf "test 316 failed";
  Gc.compact ();
  (* .b44 *)
  r.b44 <- next_r.b44;
  let r_expected = { r_expected with b44 = next_r.b44 } in
  mark_test_run 317;
  let test = eq r r_expected in
  if not test then failwithf "test 317 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a44 = #{ a43; b43 }; b44 } = r in
  let expected_a43 = #100. in
  mark_test_run 318;
  let test = (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) expected_a43 a43 in
  if not test then failwithf "test 318 failed";
  let expected_b43 = 101. in
  mark_test_run 319;
  let test = (fun a b -> Float.equal (globalize a) (globalize b)) expected_b43 b43 in
  if not test then failwithf "test 319 failed";
  let expected_b44 = #102. in
  mark_test_run 320;
  let test = (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) expected_b44 b44 in
  if not test then failwithf "test 320 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a44; b44 } = r in
  let expected_a44 = #{ a43 = #100.; b43 = 101. } in
  mark_test_run 321;
  let test = (fun #{ a43 = a431; b43 = b431 } #{ a43 = a432; b43 = b432 } -> (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) a431 a432 && (fun a b -> Float.equal (globalize a) (globalize b)) b431 b432) expected_a44 a44 in
  if not test then failwithf "test 321 failed";
  let expected_b44 = #102. in
  mark_test_run 322;
  let test = (fun a b -> Float_u.(equal (add #0. a) (add #0. b))) expected_b44 b44 in
  if not test then failwithf "test 322 failed";

  ()
;;
let () = to_run ();;

let to_run () =
  (*********************************************)
  (*   t46 = { #{ string; string }; string }   *)
  (*********************************************)
  let r = stack_ { a46 = #{ a45 = "0"; b45 = "1" }; b46 = "2" } in
  (* 1. Test field get *)
  (* Paths of depth 1 *)
  (* .a46 *)
  let actual = r.a46 in
  let expected = #{ a45 = "0"; b45 = "1" } in
  mark_test_run 323;
  let test = (fun #{ a45 = a451; b45 = b451 } #{ a45 = a452; b45 = b452 } -> (fun a b -> String.equal (globalize a) (globalize b)) a451 a452 && (fun a b -> String.equal (globalize a) (globalize b)) b451 b452) actual expected in
  if not test then failwithf "test 323 failed";
  (* Paths of depth 2 *)
  (* .a46.#a45 *)
  let actual = r.a46.#a45 in
  let expected = "0" in
  mark_test_run 324;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 324 failed";
  (* .a46.#b45 *)
  let actual = r.a46.#b45 in
  let expected = "1" in
  mark_test_run 325;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 325 failed";
  (* Paths of depth 1 *)
  (* .b46 *)
  let actual = r.b46 in
  let expected = "2" in
  mark_test_run 326;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) actual expected in
  if not test then failwithf "test 326 failed";
  Gc.compact ();
  (* 2. Test field set *)
  (* Change [r] to [next_r] one field at a time *)
  let eq = (fun { a46 = a461; b46 = b461 } { a46 = a462; b46 = b462 } -> (fun #{ a45 = a451; b45 = b451 } #{ a45 = a452; b45 = b452 } -> (fun a b -> String.equal (globalize a) (globalize b)) a451 a452 && (fun a b -> String.equal (globalize a) (globalize b)) b451 b452) a461 a462 && (fun a b -> String.equal (globalize a) (globalize b)) b461 b462) in
  let next_r = stack_ { a46 = #{ a45 = "100"; b45 = "101" }; b46 = "102" } in
  let r_expected = stack_ { a46 = #{ a45 = "0"; b45 = "1" }; b46 = "2" } in
  (* .a46 *)
  r.a46 <- next_r.a46;
  let r_expected = { r_expected with a46 = next_r.a46 } in
  mark_test_run 327;
  let test = eq r r_expected in
  if not test then failwithf "test 327 failed";
  Gc.compact ();
  (* .b46 *)
  r.b46 <- next_r.b46;
  let r_expected = { r_expected with b46 = next_r.b46 } in
  mark_test_run 328;
  let test = eq r r_expected in
  if not test then failwithf "test 328 failed";
  Gc.compact ();
  (* 3. Test deep matching *)
  let { a46 = #{ a45; b45 }; b46 } = r in
  let expected_a45 = "100" in
  mark_test_run 329;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_a45 a45 in
  if not test then failwithf "test 329 failed";
  let expected_b45 = "101" in
  mark_test_run 330;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_b45 b45 in
  if not test then failwithf "test 330 failed";
  let expected_b46 = "102" in
  mark_test_run 331;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_b46 b46 in
  if not test then failwithf "test 331 failed";
  Gc.compact ();
  (* 4. Test shallow matching *)
  let { a46; b46 } = r in
  let expected_a46 = #{ a45 = "100"; b45 = "101" } in
  mark_test_run 332;
  let test = (fun #{ a45 = a451; b45 = b451 } #{ a45 = a452; b45 = b452 } -> (fun a b -> String.equal (globalize a) (globalize b)) a451 a452 && (fun a b -> String.equal (globalize a) (globalize b)) b451 b452) expected_a46 a46 in
  if not test then failwithf "test 332 failed";
  let expected_b46 = "102" in
  mark_test_run 333;
  let test = (fun a b -> String.equal (globalize a) (globalize b)) expected_b46 b46 in
  if not test then failwithf "test 333 failed";

  ()
;;
let () = to_run ();;

for i = 1 to 333 do
  if not (Int_set.mem i !tests_run) then failwithf "test %d not run" i
done;;
let () = Printf.printf "All tests passed.%!\n";;
